import { createStitches } from "@stitches/react"

export const {
  config,
  styled,
  globalCss,
  keyframes,
  getCssText,
  theme,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      purple: "#8257e6",
    },
  },
})
